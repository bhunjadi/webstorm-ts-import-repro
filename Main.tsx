import React from 'react';
import {sum} from "/utils";

export const Mail = props => {
    // 1. uncomment useState line
    // 2. position cursor on useState (at the end) and trigger completion (Ctrl + Space)
    // 3. import clause "import {useState} from "/node_modules/@types/react";" is added
    // const [value, setValue] = useState();
    return <div>{sum(1, 4)}</div>;
};
